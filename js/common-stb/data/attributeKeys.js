/**
 * @fileoverview 属性キー定義（後方互換用再エクスポート）
 *
 * 定義本体は constants/attributeKeys.js に移動しました。
 * L1(data) → L3(common-stb) のレイヤー違反解消のためです。
 *
 * @module common-stb/data/attributeKeys
 */

export {
  WIDTH_KEYS,
  HEIGHT_KEYS,
  DIAMETER_KEYS,
  THICKNESS_KEYS,
  EXTENDED_PILE_KEYS,
} from '../../constants/attributeKeys.js';
